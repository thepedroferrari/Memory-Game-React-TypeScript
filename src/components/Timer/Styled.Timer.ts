import styled from "styled-components"

export const StyledTimer = styled.time`
  font-family: var(--heading-font);
  font-size: 1.5rem;
  place-content: center;
  place-items: center;
  svg {
    width: 1.75rem;
    height: 1.75rem;
    padding-top: 0.5rem;
  }
  span {
    line-height: 1;
  }
`
